-- SQL sript that create a tabel called first table in current database in sql server
-- description: first_table with, id int, name varchar
CREATE table if not exists first_table (
    id INT,
    name varchar(150)
)